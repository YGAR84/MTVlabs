var x : int;
var y : int;
var z : int;

{
	(z >= 0)
}

{
	x := 0;
	y := 0;
	while (y <= z)
	{
		y := (y + (x + (x + 1)));
		x := (x + 1)
	}
	{
		(((y == (x * x)) & ( ( ( (x - 1) * (x - 1) ) <= z )  V  (x == 0)  )) & (z >= 0))
	};

	x := (x - 1);
	print(x)
}

{
	( ((x * x) <= z) & (((x + 1) * (x + 1)) > z))
}

